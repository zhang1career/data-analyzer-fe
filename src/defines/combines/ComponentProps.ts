import {StylableProps} from "@/defines/abilities/StylableProps.ts";
import {DerivableProps} from "@/defines/abilities/DerivableProps.ts";

export interface ComponentProps extends StylableProps, DerivableProps {
}